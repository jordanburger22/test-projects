import React from 'react';

const Ground = () => {
  const groundStyle = {
    position: 'absolute',
    width: '100%',
    height: '60px',
    bottom: 0,
    backgroundColor: 'brown',
  };

  return <div style={groundStyle}></div>;
};

export default Ground;
